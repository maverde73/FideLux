/// Global constants for the FideLux application.
class AppConstants {
  AppConstants._();

  /// Application name.
  static const String appName = 'FideLux';

  /// Application version.
  static const String appVersion = '1.0.0';

  /// Default locale.
  static const String defaultLocale = 'it';

  /// Supported locales.
  static const List<String> supportedLocales = ['en', 'it'];
}
